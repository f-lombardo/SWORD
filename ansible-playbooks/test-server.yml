---
  - name: Test Ansible Playbook
    hosts: all
    gather_facts: no
    tasks:
      - name: Print Hello World
        ansible.builtin.command:
          cmd: hostname
        register: result

      - name: Show result
        ansible.builtin.debug:
          var: result.stdout
